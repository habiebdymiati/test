#include <iostream>
#include <cmath>
#include <cstdio>
using namespace std;

int main ( ) {
	double n;
	cin >> n;
	double fl=floor(n);
	double cl=ceil(n);
	cout << fl <<" "<<cl<<endl;
	return 0;
}
